<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>VMware Fusion虚拟机避开虚拟机检测</title>
    <url>/2019/11/28/VMware-Fusion%E8%99%9A%E6%8B%9F%E6%9C%BA%E9%81%BF%E5%BC%80%E8%99%9A%E6%8B%9F%E6%9C%BA%E6%A3%80%E6%B5%8B/</url>
    <content><![CDATA[<blockquote>
<p>部分软件启动后会检测其是否运行在虚拟机中,如果是则退出，下面给出方案避开虚拟机检测。</p>
</blockquote><p>在虚拟机未启动的情况下（如果虚拟机开着不能保存配置）打开 Finder 在上边的搜索框里输入 VMX 就会搜索到一个以.vmx 结尾的文件</p><p>这个文件就是虚拟机的配置文件，如果有多个虚拟机请根据你想配置的虚拟机名字来配置。</p><a id="more"></a>


<p>目录因人而异，注意将<code>anchor</code>替换为自己的用户名，我的目录是：</p>
<pre><code>/Users/anchor/Documents/VirtualMachines.localized/Windows7x64.vmwarevm/Windows7x64.vmx</code></pre><p>之后用文本编辑器打开这个配置文件，在末尾加上：</p>
<pre><code>monitor_control.restrict_backdoor = &quot;TRUE&quot;
isolation.tools.setVersion.disable = &quot;TRUE&quot;
isolation.tools.getVersion.disable = &quot;TRUE&quot;</code></pre><p>保存文件，启动虚拟机。搞定。</p>
]]></content>
      <categories>
        <category>MacBook</category>
      </categories>
      <tags>
        <tag>VMware</tag>
        <tag>虚拟机</tag>
        <tag>虚拟检测</tag>
      </tags>
  </entry>
  <entry>
    <title>安装HomeBrew</title>
    <url>/2019/11/28/%E5%AE%89%E8%A3%85HomeBrew/</url>
    <content><![CDATA[<h1 id="1、简介"><a href="#1、简介" class="headerlink" title="1、简介"></a>1、简介</h1><p><a href="https://brew.sh/index_zh-cn.html" target="_blank" rel="noopener">HomeBrew 官网</a></p>
<p><code>Homebrew</code>是一个包管理器，用于安装 Apple 没有预装但你需要的 UNIX 工具。（比如著名的<code>wget</code>）。</p>
<p><code>Homebrew</code>会将软件包安装到独立目录(<code>/usr/local/Cellar</code>)，并将其文件软链接至<code>/usr/local</code>。</p>
<p><code>Homebrew</code>不会将文件安装到它本身目录之外，所以可将<code>Homebrew</code>安装到任意位置。</p>
<a id="more"></a>

<h1 id="2、安装"><a href="#2、安装" class="headerlink" title="2、安装"></a>2、安装</h1><ul>
<li><p>安装<code>XCode</code>或者<code>Command Line Tools for Xcode</code>。<code>Xcode</code>可以从<code>AppStore</code>里下载安装，<code>Command Line Tools for Xcode</code>可以终端命令安装</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">xcode-select --install</span><br></pre></td></tr></table></figure>
</li>
<li><p>安装 Homebrew</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/usr/bin/ruby -e &quot;$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)&quot;</span><br></pre></td></tr></table></figure>

<p>脚本会在执行前暂停，并说明将它将做什么。</p>
<blockquote>
<p>安装完成以后，需要运行 brew<br>doctor 命令检测下是否有什么冲突的地方（如没有卸载 MacPorts 等等）<br>Homebrew 安装成功后，会自动创建目录/usr/local/Cellar 来存放 Homebrew 安装的程序。</p>
</blockquote>
</li>
<li><p>移除 Homebrew</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/usr/bin/ruby -e &quot;$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/uninstall)&quot;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h1 id="3、常用命令"><a href="#3、常用命令" class="headerlink" title="3、常用命令"></a>3、常用命令</h1><pre><code>// 搜索包
brew search mysql

// 安装包
brew install mysql

// 查看包信息，比如目前的版本，依赖，安装后注意事项等
brew info mysql

// 卸载包
brew uninstall wget

// 显示已安装的包
brew list

// 查看brew的帮助
brew –help

// 更新， 这会更新 Homebrew 自己
brew update

// 检查过时（是否有新版本），这会列出所有安装的包里，哪些可以升级
brew outdated
brew outdated mysql

// 升级所有可以升级的软件们
brew upgrade
brew upgrade mysql

// 清理不需要的版本极其安装包缓存
brew cleanup
brew cleanup mysql</code></pre>]]></content>
      <categories>
        <category>MacBook</category>
      </categories>
      <tags>
        <tag>MacBook</tag>
        <tag>HomeBrew</tag>
      </tags>
  </entry>
  <entry>
    <title>Mybatis一级缓存导致的内存溢出（二）</title>
    <url>/2018/09/29/Mybatis%E4%B8%80%E7%BA%A7%E7%BC%93%E5%AD%98%E5%AF%BC%E8%87%B4%E7%9A%84%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BA%EF%BC%88%E4%BA%8C%EF%BC%89/</url>
    <content><![CDATA[<blockquote>
<p>接上篇 <a href="http://genji.wiki/2018/08/06/Druid%E7%9B%91%E6%8E%A7%E5%AF%BC%E8%87%B4%E7%9A%84%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BA%E9%97%AE%E9%A2%98%E5%AE%9A%E4%BD%8D%E5%A4%84%E7%90%86%EF%BC%88%E4%B8%80%EF%BC%89/">Druid 监控导致的内存溢出问题定位处理（一）</a> JVM 内存溢出相关，废话不多说直接上分析过程。</p>
</blockquote><h1 id="1、服务器内存快照分析"><a href="#1、服务器内存快照分析" class="headerlink" title="1、服务器内存快照分析"></a>1、服务器内存快照分析</h1><p>首先通过快照查看最大的二十个对象如下图：<br><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129201040.png" alt></p><p>根据反馈数据可以发现前十个对象大小基本差不多，猜测应该由一个问题导致，点击去进一步定位问题。</p><h1 id="2、定位引起内存溢出的关键对象"><a href="#2、定位引起内存溢出的关键对象" class="headerlink" title="2、定位引起内存溢出的关键对象"></a>2、定位引起内存溢出的关键对象</h1><p>发现引起内存溢出的对象很明确为 mybatis 的 PerpetualCache 对象：<br><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129201323.png" alt><br><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129201335.png" alt></p><a id="more"></a>



<h1 id="3、分析引起内存溢出的原因"><a href="#3、分析引起内存溢出的原因" class="headerlink" title="3、分析引起内存溢出的原因"></a>3、分析引起内存溢出的原因</h1><ul>
<li><p>猜测溢出</p>
<p><code>PerpetualCache</code> 其实是 Mybatis 的一级缓存对象，且全部为 <code>HashMap</code> 类型对象，所以可以大胆猜测是由于 Mybatis 一级缓存不正确使用造成的，敢于如此猜测是因为：</p>
<p>MyBatis 对会话（Session）级别的一级缓存设计的比较简单，就简单地使用了 HashMap 来维护，并没有对 HashMap 的容量和大小进行限制。</p>
<p>所以如果我们一直使用某一个 SqlSession 对象查询数据，这样会不会导致 HashMap 太大，而导致 java.lang.OutOfMemoryError 错误？</p>
</li>
</ul>
<ul>
<li><p>验证溢出</p>
<p>Mybatis 的一级缓存是事务级别的，一旦事务提交就会清空。因此可能该线程的事务一直未结束和提交。那就从一个拥有大量查询业务繁杂的功能入手验证，结合二阶段定位到的零星线索，发现服务器内存溢出时正在处理的业务是数据下发，那我们就以次为入手点验证：</p>
<p>A.首先设置 JVM 参数，为了尽快实现内存溢出效果，设定一个合适的参数，可根据本地环境调整：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-server -XX:PermSize=256M -XX:MaxPermSize=256m</span><br><span class="line">-Xms300M -Xmx300M</span><br><span class="line">-Dcom.sun.management.jmxremote.port=8999</span><br><span class="line">-Dcom.sun.management.jmxremote.ssl=false</span><br><span class="line">-Dcom.sun.management.jmxremote.authenticate=false</span><br><span class="line">-XX:+HeapDumpOnOutOfMemoryError</span><br><span class="line">-XX:HeapDumpPath=/Users/anchor/Downloads/dump/</span><br></pre></td></tr></table></figure>

<p>B.开启 Jvisualvm 监控，应用中开启下发业务，获取监控数据：</p>
<p>如下图所示可以看到，本地程序在运行一段时间后已经处于假死状态，GC 回收活动频繁，可堆内存基本未见有效回收，CPU 过山车式的起伏，很快内存溢出了，至此可以得出结论我们找对了入手点：</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129201838.png" alt></p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129201855.png" alt></p>
</li>
</ul>
<ul>
<li><p>修改溢出</p>
<p>加断点分析下发业务发现，下发业务存在大量 for 循环引发大量数据库查询操作，且该操作参数不重复，也就导致了 Mybatis 的一级缓存一直无法命中，所以正好验证我们的猜测，在一次下发请求中 Mybatis 创建了海量的缓存数据，改部分数据在下发业务完成之前是无法释放的，占用了大量的内存资源，如果此时刚好服务器内存有限很容易引发 GC overhead limit exceded 错误，进而造成服务假死内存溢出：</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129202001.png" alt></p>
<p>结合项目实际情况针对一级缓存的可利用率实在过低，由于参数一直变动，缓存命中几率极低，从 MyBatis 一级缓存来看，它以单纯的 HashMap 做缓存，没有容量控制，而一次 SqlSession 中通常来说并不会有大量的查询操作，因此只适用于一次 SqlSession，且项目中已开启二级缓存，所以我们考虑修改一级缓存规则：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!-设置一级缓存模式为STATEMENT，默认为SESSION，SESSION为会话级别，STATEMENT为SQL级别-&gt;</span><br><span class="line">&lt;setting name=&quot;localCacheScope&quot; value=&quot;STATEMENT&quot;/&gt;</span><br><span class="line">&lt;!-PS：一级缓存的范围有SESSION和STATEMENT两种，默认是SESSION，如果我们不需要使用一级缓存，那么我们可以把一级缓存的范围指定为STATEMENT，这样每次执行完一个Mapper语句后都会将一级缓存清除。如果需要更改一级缓存的范围，请在Mybatis的配置文件中，在&lt;settings&gt;下通过localCacheScope指定。-&gt;</span><br></pre></td></tr></table></figure>
</li>
<li><p>再次验证</p>
<p>启动项目，开启下发业务，打开监控，效果如下：</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129202204.png" alt></p>
<p>818 家门店下发业务正常完成，期间堆内存回收稳定，无溢出情况出现（坡度较陡是因为本地 JVM 内存设置较低，为提高内存利用率，GC 回收频繁）。</p>
</li>
</ul>
<h1 id="4、总结"><a href="#4、总结" class="headerlink" title="4、总结"></a>4、总结</h1><ul>
<li><p>疑问：既然一级缓存会导致内存溢出，为什么 Mybatis 还如此设计？</p>
<p>MyBatis 这样设计也有它自己的理由：</p>
<blockquote>
<p>a.一般而言 <code>SqlSession</code> 的生存时间很短。一般情况下使用一个 <code>SqlSession</code> 对象执行的操作不会太多，执行完就会消亡；</p>
</blockquote>
<blockquote>
<p>b.对于某一个 <code>SqlSession</code> 对象而言，只要执行 <code>update</code> 操作（<code>update</code>、<code>insert</code>、<code>delete</code>），都会将这个 <code>SqlSession</code> 对象中对应的一级缓存清空掉，所以一般情况下不会出现缓存过大，影响 JVM 内存空间的问题；</p>
</blockquote>
<blockquote>
<p>c.可以手动地释放掉 <code>SqlSession</code> 对象中的缓存。</p>
</blockquote>
</li>
<li><p>收获：Mybatis 一级缓存的存储流程</p>
<blockquote>
<p>a.<code>MyBatis</code> 的一级缓存是 <code>SqlSession</code> 级别的，但是它并不定义在 <code>SqlSessio</code> 接口的实现类 <code>DefaultSqlSession</code> 中，而是定义在 <code>DefaultSqlSession</code> 的成员变量 <code>Executor</code> <code>中，Executor</code> 是在 <code>openSession</code> 的时候被实例化出来的，它的默认实现为 <code>SimpleExecutor。</code></p>
</blockquote>
<blockquote>
<p>b.<code>MyBatis</code> 中的一级缓存，与有没有配置无关，只要 <code>SqlSession</code> <code>存在，MyBastis</code> <code>一级缓存就存在，localCache</code> 的类型是 <code>PerpetualCache，它其实很简单，一个</code> id 属性+一个 <code>HashMap</code> 属性而已，id 是一个名为”localCache”的字符串，HashMap 用于存储数据，Key 为 <code>CacheKey</code>，<code>Value</code> 为查询结果。</p>
</blockquote>
<blockquote>
<p>c.<code>MyBatis</code> 的一级缓存查询的时候默认都是会先尝试从一级缓存中获取数据的，即想每次查询都走 DB 也行，将<code>&lt;select&gt;</code>标签中的 <code>flushCache</code> 属性设置为 <code>true</code> 即可，这意味着每次查询的时候都会清理一遍 <code>PerpetualCache</code>，<code>PerpetualCache</code> 中没数据，自然只能走 DB。</p>
</blockquote>
</li>
</ul>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>OOM</tag>
        <tag>Mybatis</tag>
      </tags>
  </entry>
  <entry>
    <title>Druid监控导致的内存溢出问题定位处理（一）</title>
    <url>/2018/08/06/Druid%E7%9B%91%E6%8E%A7%E5%AF%BC%E8%87%B4%E7%9A%84%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BA%E9%97%AE%E9%A2%98%E5%AE%9A%E4%BD%8D%E5%A4%84%E7%90%86%EF%BC%88%E4%B8%80%EF%BC%89/</url>
    <content><![CDATA[<h1 id="0、起因"><a href="#0、起因" class="headerlink" title="0、起因"></a>0、起因</h1><p>一台部署于商户私有云中的应用频繁宕机，经查为 OOM，周期大约一周一次，较规律，所谓千里之堤毁于蚁穴，初步怀疑是轻微的内存泄漏不断积累导致的内存溢出，所以再次宕机出现时果断 dump 内存镜像开始分析</p>
<a id="more"></a>

<h1 id="1、内存溢出原理"><a href="#1、内存溢出原理" class="headerlink" title="1、内存溢出原理"></a>1、内存溢出原理</h1><ul>
<li><p>常见内存溢出：</p>
<p>相信通过写 java 程序讨生活的人对内存溢出并不陌生，先看三张图：</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129154638.png" alt=""></p>
<p>Heap space（堆空间）</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129155003.png" alt=""></p>
<p>Permgen（永久代）</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129155026.png" alt=""></p>
<p>元空间（Metaspace）</p>
</li>
<li><p>JVM 内存模型</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129155306.png" alt=""></p>
<p>Java 应用程序在启动时会指定所需要的内存大小，它被分割成两个不同的区域：<code>Heap space</code>（堆空间）和<code>Permgen</code>（永久代）。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">java.lang.OutOfMemoryError</span><br></pre></td></tr></table></figure>

<p>正如字面意思，当应用程序试图向这两个空间添加更多的数据，但却没有足够的空间来容纳这些数据时，将会触发<code>java.lang.OutOfMemoryError</code>。</p>
</li>
<li><p>内存溢出原因分析</p>
<p>1、java.lang.OutOfMemoryError: PermGen space</p>
<p><code>PermGen space</code>的全称是<code>Permanent Generation space</code>,是指内存的永久保存区域, 这块内存主要是被 JVM 存放 Class 和 Meta 信息的,Class 在被 Load 时就会被放到<code>PermGen space</code>中, 同时 GC(Garbage Collection)不会在主程序运行期对 <code>PermGen space</code>进行清理。</p>
<p>所以如果你的应用中有很多 CLASS 的话,就很可能出现 PermGen space 错误。</p>
<p>通过上面的描述就可以得出：如果要加载的 class 与 jar 文件大小超过-XX:MaxPermSize 就有可能会产生 java.lang.OutOfMemoryError: PermGen space 。</p>
<p>换句话说-XX:MaxPermSize 的大小要超过 class 与 jar 的大小。</p>
<p>2、java.lang.OutOfMemoryError: Java heap space</p>
<p>虽然各种 java 虚拟机的实现机制不一,但是<code>heap space</code>内存溢出产生的原因相同：那就是堆内存不够虚拟机分配了。</p>
<p>由于内存的分配机制与 GC 是有联系的，也就是说内存不够用时有部分内存 GC 肯定无法释放，而我们要找的就是为什么 GC 释放不了堆内存。</p>
<p>所以当产生<code>heap space</code>内存溢出时，堆内存中对象数量过多的就可能是问题的根源了。例外的情况是，程序确实需要那么多内存，这时就要考虑增大堆内存。</p>
</li>
</ul>
<h1 id="2、内存溢出时应该做什么"><a href="#2、内存溢出时应该做什么" class="headerlink" title="2、内存溢出时应该做什么"></a>2、内存溢出时应该做什么</h1><ul>
<li><p>收集案发现场数据，尽快恢复系统</p>
<p>获取内存快照，根据适用场景有以下三种方式：</p>
<p>1、使用 jmap 命令在案发现场手动生成 dump 文件</p>
<p><code>jmap -dump:format=b,file=heap.dump \`pid of java\`</code></p>
<p>2、使用 JVM 参数自动在案发当场获取 dump 文件</p>
<p><code>-XX:+HeapDumpOnOutOfMemoryError</code></p>
<p>3、使用其它工具获取 dump 文件，主要针对本地开发环境：</p>
<p>分析 <code>Heap Dump</code> 的工具都可以获取 <code>Heap Dump</code> 文件。<br>比如：<code>jdk</code> 自带的工具 <code>jvisualvm</code>。<br>其它工具：<code>Eclipse memory analyzer（jmat）</code>、<code>JProfiler</code> 等。</p>
</li>
<li><p>本地复现之 jvisualvm 的使用</p>
<p>无论是为了再现案发场景还是验证修复效果，都需要我们在本地环境中监控 JVM 的运行情况，SO 以我熟悉的<code>jvisualvm</code>为例，简单说下使用方法</p>
<blockquote>
<p>VisualVM 提供在 Java 虚拟机 (Java Virutal Machine, JVM) 上运行的 Java 应用程序的详细信息。在 VisualVM 的图形用户界面中，您可以方便、快捷地查看多个 Java 应用程序的相关信息。（摘自官方）<br>简单说来，VisualVM 是一种集成了多个 JDK 命令行工具的可视化工具，它能为您提供强大的分析能力。所有这些都是免费的！它囊括的命令行工具包括 jstat, JConsole, jstack, jmap 和 jinfo，这些工具与 JDK 的标准版本是一致的。 可以使用 VisualVM 生成和分析海量数据、跟踪内存泄漏、监控垃圾回收器、执行内存和 CPU 分析。尽管 VisualVM 自身要在 JDK6 这个版本上运行，但是 JDK1.4 以上版本的程序它都能监控。<br>VisualVM 的一个最大好处就是，它已经在你的 JDK bin 目录里了，只要你使用的是 JDK1.6 Update7 之后的版本。点击一下 jvisualvm.exe 图标它就可以运行了，也可以终端或者 CMD 中使用 jvisualvm 命令启动。</p>
</blockquote>
<p>Jvisualvm 本地联调的两种常用方法：</p>
<p>1、<code>Jvisualvm</code>与<code>IDEA</code>集成，<code>Intelij Idea</code> -&gt; <code>Preferences</code> -&gt; <code>Plugins</code> -&gt;<br><code>Browers Repositrories</code>，搜索<code>visualvm</code>，选择<code>install</code>：安装成功后会添加<code>visualvm</code>的启动选项，启动 Tomcat 的同时会打开一个<code>VisualVM</code>的窗口。</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129161407.png" alt=""></p>
<p>2、不通过插件直接启动，与本地 Tomcat 联调，需要配置一下 Tomcat，添加参数：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-Dcom.sun.management.jmxremote.port=8999</span><br><span class="line">-Dcom.sun.management.jmxremote.ssl=false</span><br><span class="line">-Dcom.sun.management.jmxremote.authenticate=false</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h1 id="3、自造案发现场"><a href="#3、自造案发现场" class="headerlink" title="3、自造案发现场"></a>3、自造案发现场</h1><blockquote>
<p>为了快速验证我们的工具以及加深对 OOM 的理解，我们自造几种内存溢出场景，代码我已准备妥当，可以直接复制运行验证，开始前记得根据示例调整 JVM 参数~</p>
</blockquote>
<ul>
<li><p>PermGen OOM</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * @program: jvisualvmDemo</span><br><span class="line"> * @description: jdk1.7永久区内存溢出</span><br><span class="line"> * -XX:MaxPermSize=5m</span><br><span class="line"> * -XX:+HeapDumpOnOutOfMemoryError</span><br><span class="line"> * -XX:HeapDumpPath=/Users/anchor/Downloads/dump/</span><br><span class="line"> * @author: Anchor</span><br><span class="line"> * @create: 2018-07-31</span><br><span class="line"> **/</span><br><span class="line">public class PermGenOOM &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        while (true) &#123;</span><br><span class="line">            Enhancer enhancer = new Enhancer();</span><br><span class="line">            enhancer.setSuperclass(MetaOOM.OOMObject.class);</span><br><span class="line">            enhancer.setUseCache(false);</span><br><span class="line">            enhancer.setCallback(new MethodInterceptor() &#123;</span><br><span class="line">                public Object intercept(Object obj, Method method, Object[] args, MethodProxy proxy) throws Throwable &#123;</span><br><span class="line">                    return proxy.invokeSuper(obj, args);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">            //无限创建动态代理，生成Class对象</span><br><span class="line">            enhancer.create();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    static class OOMObject &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>MetaOOM</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * @program: jvisualvmDemo</span><br><span class="line"> * @description: 方法区内存异常</span><br><span class="line"> * -XX:MetaspaceSize=8m</span><br><span class="line"> * -XX:MaxMetaspaceSize=8m</span><br><span class="line"> * -XX:+HeapDumpOnOutOfMemoryError</span><br><span class="line"> * -XX:HeapDumpPath=/Users/anchor/Downloads/dump/</span><br><span class="line"> * @author: Anchor</span><br><span class="line"> * @create: 2018-07-27</span><br><span class="line"> **/</span><br><span class="line">public class MetaOOM &#123;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        while (true) &#123;</span><br><span class="line">            Enhancer enhancer = new Enhancer();</span><br><span class="line">            enhancer.setSuperclass(OOMObject.class);</span><br><span class="line">            enhancer.setUseCache(false);</span><br><span class="line">            enhancer.setCallback(new MethodInterceptor() &#123;</span><br><span class="line">                public Object intercept(Object obj, Method method, Object[] args, MethodProxy proxy) throws Throwable &#123;</span><br><span class="line">                    return proxy.invokeSuper(obj, args);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">            //无限创建动态代理，生成Class对象</span><br><span class="line">            enhancer.create();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    static class OOMObject &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>HeapOOM</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * @program: jvisualvmDemo</span><br><span class="line"> * @description: 堆内存异常</span><br><span class="line"> * VM Args:</span><br><span class="line"> *     //这两个参数保证了堆中的可分配内存固定为20M</span><br><span class="line"> *     -Xms20m</span><br><span class="line"> *     -Xmx20m</span><br><span class="line"> *     -XX:+HeapDumpOnOutOfMemoryError</span><br><span class="line"> *     -XX:HeapDumpPath=/Users/anchor/Downloads/dump/</span><br><span class="line"> * @author: Anchor</span><br><span class="line"> * @create: 2018-07-27</span><br><span class="line"> **/</span><br><span class="line">public class HeapOOM &#123;</span><br><span class="line">    //创建一个内部类用于创建对象使用</span><br><span class="line">    static class OOMObject &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        List&lt;OOMObject&gt; list = new ArrayList&lt;OOMObject&gt;();</span><br><span class="line">        //无限创建对象，在堆中</span><br><span class="line">        while (true) &#123;</span><br><span class="line">            list.add(new OOMObject());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>JavaVMStackSOF</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * @program: jvisualvmDemo</span><br><span class="line"> * @description: 栈内存异常</span><br><span class="line"> * VM Args：</span><br><span class="line"> *     //设置栈容量为160K，默认1M</span><br><span class="line"> *    -Xss160k</span><br><span class="line"> * @author: Anchor</span><br><span class="line"> * @create: 2018-07-27</span><br><span class="line"> **/</span><br><span class="line">public class JavaVMStackSOF &#123;</span><br><span class="line">    private int stackLength = 1;</span><br><span class="line">    public void stackLeak() &#123;</span><br><span class="line">        stackLength++;</span><br><span class="line">        //递归调用，触发异常</span><br><span class="line">        stackLeak();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) throws Throwable &#123;</span><br><span class="line">        JavaVMStackSOF oom = new JavaVMStackSOF();</span><br><span class="line">        try &#123;</span><br><span class="line">            oom.stackLeak();</span><br><span class="line">        &#125; catch (Throwable e) &#123;</span><br><span class="line">            System.out.println(&quot;stack length:&quot; + oom.stackLength);</span><br><span class="line">            throw e;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h1 id="4、案例分析"><a href="#4、案例分析" class="headerlink" title="4、案例分析"></a>4、案例分析</h1><blockquote>
<p>啰里啰嗦这么终于要进入正题了 😹</p>
</blockquote>
<ul>
<li><p>准备环境，测试脚本</p>
<p>根据本地机器性能配置 JVM 参数：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-server -XX:PermSize=256M -XX:MaxPermSize=256m -Xms300M -Xmx300M</span><br><span class="line">-Dcom.sun.management.jmxremote.port=8999</span><br><span class="line">-Dcom.sun.management.jmxremote.ssl=false</span><br><span class="line">-Dcom.sun.management.jmxremote.authenticate=false</span><br></pre></td></tr></table></figure>

<p>准备测试脚本：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#!/usr/bin/env python3</span><br><span class="line"># -*- coding: utf-8 -*-</span><br><span class="line"></span><br><span class="line">from multiprocessing import Process,Pool</span><br><span class="line">import os,time</span><br><span class="line">import requests</span><br><span class="line"></span><br><span class="line">def run_proc(name):</span><br><span class="line">    for i in range(30000):</span><br><span class="line">        postdata = &#123;&apos;code&apos;: i&#125;</span><br><span class="line">        r = requests.post(&quot;http://localhost:8080/OOM.do&quot;, data=postdata)</span><br><span class="line"></span><br><span class="line">if __name__ ==&apos;__main__&apos;:</span><br><span class="line">    print(&apos;Run the main process (%s).&apos; % (os.getpid()))</span><br><span class="line">    mainStart = time.time()</span><br><span class="line">    p = Pool(8)</span><br><span class="line">    for i in range(16):</span><br><span class="line">        p.apply_async(run_proc,args=(&apos;Process&apos;+str(i),))</span><br><span class="line"></span><br><span class="line">    print(&apos;Waiting for all subprocesses done ...&apos;)</span><br><span class="line">    p.close()</span><br><span class="line">    p.join()</span><br><span class="line">    print(&apos;All subprocesses done&apos;)</span><br><span class="line">    mainEnd = time.time()</span><br><span class="line">    print (&apos;All process ran %0.2f seconds.&apos; % (mainEnd-mainStart))</span><br></pre></td></tr></table></figure>
</li>
<li><p>内存快照分析</p>
<p>快照导入 visualvm，简单翻了一下很快发现问题，内存中存在大量 SQL 片段未回收，如图：</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129163115.png" alt=""></p>
<p>为何内存会被大量 SQL 片段占领，莫非？？？果断开启断言（瞎猜）模式，我们要复现问题！</p>
<blockquote>
<p>假设 A：使用了静态变量定义 SQL 片段，导致资源未回收<br>翻代码确认，发现 SQL 使用 String 对象拼接，非静态变量，但是该 SQL 进行了上百次的拼接必然造成资源浪费加重 GC 负担,有优化空间改 StringBuild，测试之。</p>
</blockquote>
<p>脚本跑起来~ Emm~~ 貌似没啥效果</p>
<blockquote>
<p>假设 B：连接池不够用？</p>
</blockquote>
<p>翻代码确认，发现配置正常，改小连接池大小，测试之，无效。</p>
<blockquote>
<p>假设 C：数据库有死锁？</p>
</blockquote>
<p>但是与运维确认每次内存溢出只需重启应用即可恢复，数据库无需操作，PASS</p>
<blockquote>
<p>假设 D：。。。。。</p>
</blockquote>
<blockquote>
<p>假设 X：难道 Druid 连接池会导致内存溢出？</p>
</blockquote>
<p>检查 Druid 配置，发现代码中除了使用了连接池还配置了 SQL 监控，URL 监控，那么监控数据存储在哪里了？难道默认是内存中？百度 Google 无果，直接动手压测，模拟 50W 请求，得到如下数据：成功的看到了内存溢出。</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129164503.png" alt=""></p>
<p>对比生产机内存快照，发现有戏！组成结果基本一致，可是没道理啊，难度 Druid 还有这种大坑？不行不敢确定，那么我们反向验证下，如果我关掉 Druid 的监控呢？说干就干，再跑 50W 请求：</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129164702.png" alt=""></p>
<p>欧克，果然有点意思，OOM 消失了(๑•̀ㅂ•́)و✧</p>
<p>既然问题找到了，那么就要分析为啥会出现这种大坑，先从官网入手，翻下 Druid 的 lssues 发现竟然有前辈已经问过该类问题</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129165007.png" alt=""></p>
<p>but，官方并未回复，那么看起来这个问题太 low？</p>
<p>Emm…. 那自己研究下好了</p>
</li>
<li><p>进一步扩展</p>
<p>翻阅官方文档， <a href="https://github.com/alibaba/druid/wiki/%E9%85%8D%E7%BD%AE_StatFilter" target="_blank" rel="noopener">在 SQL 合并配置</a> ，发现如下内容：</p>
<blockquote>
<p>当你程序中存在没有参数化的 sql 执行时，sql 统计的效果会不好。比如：<br>select _ from t where id = 1<br>select _ from t where id = 2<br>select _ from t where id = 3<br>在统计中，显示为 3 条 sql，这不是我们希望要的效果。StatFilter 提供合并的功能，能够将这 3 个 SQL 合并为如下的 SQL<br>select _ from t where id = ?</p>
</blockquote>
<p>问题到此已经基本明了，不规范的入参导致<code>StatFilter</code>未能成功合并 SQL，Druid 成功记录了所有的执行语句。。。。。。。</p>
<p>大家可以自行体会下这两种写法的区别：</p>
<p><img src="https://genji-1256166567.cos.ap-chengdu.myqcloud.com/genji/20191129165827.png" alt=""></p>
</li>
</ul>
<h1 id="5、内存溢出排查总结"><a href="#5、内存溢出排查总结" class="headerlink" title="5、内存溢出排查总结"></a>5、内存溢出排查总结</h1><blockquote>
<p>导致 OutOfMemoryError 异常的常见原因有以下几种：</p>
<pre><code>- 内存中加载的数据量过于庞大，如一次从数据库取出过多数据；
- 集合类中有对对象的引用，使用完后未清空，使得JVM不能回收；
- 代码中存在死循环或循环产生过多重复的对象实体；
- 使用的第三方软件中的BUG；
- 启动参数内存值设定的过小；
- 编码不规范的隐患；</code></pre></blockquote>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>JVM</tag>
        <tag>OOM</tag>
        <tag>Druid</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2016/10/24/hello-world/</url>
    <content><![CDATA[<p>是的，Hello World!</p><p>当我们学习一门编程语言时，都是从“Hello,World!”开始。所有程序员在其职业生涯中，都至少接触过一个经典的“Hello,World!”程序。</p><p>通常程序员会使用多种编程语言，多的甚至实现了十几种。 </p><p>一直有记笔记的习惯，但是第一次尝试写blog，SO</p><p>那就让我们从Hello World开始吧！</p>]]></content>
  </entry>
</search>
